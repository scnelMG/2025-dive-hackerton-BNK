# Historical notebooks are review artifacts; an exact dependency lockfile was not preserved.
# These ranges match the documented Python 3.9+ review environment without claiming bit-for-bit reproduction.
pandas>=2.0,<3
numpy>=1.24,<3
matplotlib>=3.7,<4
seaborn>=0.12,<1
scikit-learn>=1.2,<2
jupyter>=1,<2
